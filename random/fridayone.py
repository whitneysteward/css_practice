#def hello(name, age): #name is an argument use *args and python will make a tuple
# def hello (**args)
    #print(args) <---- this will recognize how many separated by commas and
    #call the function

    #print("Hello" + str (name))
    #print (" You are " + str (age))

    #hello('Chris',33) #call a function
    #hello('Kate', 20)


#def question (answer):
    #if answer == '42':
#    print ('You are correct!')
#else:
    #print ('Nope, try again!')

#ans = input ('What is the meaning of everything?')

#question(ans)


"""Define a function calls addNumber(x, y) that takes
#in two number and returns the sum of the two numbers. """

#def addition (x, y):
    #return (x + y)
    #ans = addition (5,5)
    #print(ans)



"""Define a function calls subtractNumber(x, y) that takes in two
numbers and returns the difference of the two numbers."""

#def subtractNumber( x, y):
    #print (x - y)
    #subtractNumber(554,69)


"""Write a function getSumOfLastDigits() that takes in a list of
positive numbers and returns the sum of all the last digits in the list.
get the last digit """

 #def getSumOfLastDigits(lst):
     #last_digits = []
    #for item in lst:
        #str_item = str(item)
        #last_digits.append(int(str_item[-1])
        #print(sum(last_digits))
#getSumOfLastDigits([1222,2222,33333,3333,32])





"""Write a function getBiggerNumber(x, y) that takes
in two numbers as arguments and returns the
 bigger number."""

 #def getBiggerNumber (x, y):
    #if answer == (x > y)
#else: (y > x)

     #getBiggerNumber(554, 69)

     #def greeting (name, greet="Hello"):
         #print('{g},{n})!'.format (g=greet, n= name))

         #greeting ('Chris', 'Yo')



"""Write a function is Equilateral(x, y, z) that accepts the 3 sides of a
triangle as arguments. The program should return True if it is an equilateral
triangle."""

#def equilateral_triangle(x, y, z):
    #if x == y == z
    #return ('True!')
#else:
    #return('False!')


"""Define a function calls addFirstAndLast(x) that takes in a list of numbers
 and returns the sum of the first and last numbers."""

#def addFirstAndLast (lst):
    #last_digits = []
    #for item in lst:
        #str_item = str(item
        #last_digits.append(int(str_item [-1])))
      #addFirstAndLast([1222,2222,33333,3333,32])


"""Define a function isEven(number) that takes in a number as an
argument and returns True if it is an even number."""
#def isEven (x):
    #if (x % 2 == 0):
        #return ('True')
    #else:
        #return ('False')

    """
Write a function that converts the time to 24hr format"""

"""Write the function countA(word) that takes in a word as argument
 and returns the number of 'a' in that word."""

 def CountA (lst):
     words = []
     count =
